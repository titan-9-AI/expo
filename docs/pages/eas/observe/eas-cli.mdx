# ไปยังโฟลเดอร์โปรเจกต์ (ที่วางโค้ดแอป)
cd apex-titan9-ai

# ติดตั้งแพ็กเกจทั้งหมด (ใช้เวลา 1-3 นาที)
npm install

# ติดตั้งแพ็กเกจเฉพาะ Expo ให้ตรง SDK (สำคัญมาก — ห้ามใช้ version อื่น)
npx expo install expo-router expo-image expo-linear-gradient expo-blur \
  expo-status-bar expo-crypto expo-constants expo-splash-screen \
  expo-linking expo-system-ui expo-font expo-clipboard \
  react-native-safe-area-context @react-native-async-storage/async-storage \
  react-native-web react-dom @expo/metro-runtime

# ไอคอน + supabase
npm install lucide-react-native react-native-svg @supabase/supabase-js --legacy-peer-deps
